{
  "name": "exam-prep-root",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
  "express": "^4.18.2"
},
  "scripts": {
    "install:all": "yarn --cwd server install && yarn --cwd client install",
    "postinstall": "yarn --cwd client install --silent && yarn --cwd client run build",
    "build": "yarn install && yarn --cwd client install && yarn --cwd client run build",
    "start": "yarn --cwd server start"
  },
  "engines": {
    "node": ">=18"
  }
}
